package com.hetarth.expensetracker.enums;

/**
 * Represents security authorities granted to users.
 * In this beginner-friendly app, we default to a standard USER role.
 */
public enum Role {
    USER
}
